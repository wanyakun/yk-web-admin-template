module.exports = {

  title: 'YK Admin Template',

  /**
   * 是否显示设置
   */
  showSettings: false,

  /**
   * @type {boolean} true | false
   * @description Whether fix the header
   */
  fixedHeader: false,

  /**
   * @type {boolean} true | false
   * @description Whether show the logo in sidebar
   */
  sidebarLogo: false,

  /**
   * 是否启用权限控制，独立使用的时候建议使用权限控制，嵌入到CMP中的时候可以取消权限控制，完全交由CMP控制，也可以开启增加URL级别的控制, 默认为true
   */
  permissionEnable: true,

  /**
   * 是否显示菜单栏，独立使用的时候建议设置为true，嵌入CMP的时候设置为false，默认为true
   */
  showSidebar: true,

  /**
   * 是否显示导航栏，独立使用的时候建议设置为true，嵌入CMP的时候设置为false，默认为true
   */
  showNavbar: true,
  /**
   * 展示TagsView
   */
  tagsView: true
}
